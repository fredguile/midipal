CompileFlags:
  Add:
    - "-I.."
    - "-I../.."
    - "-I/usr/lib/avr/include"
    - "-D__AVR__"
    - "-D__AVR_ATmega328P__"
    - "-include=avrlib/avr_stub.h"

If:
  PathMatch: ".*\\.h"
  CompileFlags:
    Add:
      - "-x"
      - "c++-header"
